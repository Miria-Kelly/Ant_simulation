package org.example;

import org.lwjgl.glfw.GLFW;
import org.lwjgl.opengl.*;

/*GL_STATIC_DRAW → os dados quase nunca mudam (ex.: uma árvore, uma pedra, uma casa).
GL_DYNAMIC_DRAW → os dados mudam de vez em quando.
GL_STREAM_DRAW → os dados mudam praticamente a cada frame.
*/

public class Main {

}